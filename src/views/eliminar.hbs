<main>


<h2>
    Eliminar registro?
</h2>

<form action="/eliminado/{{ result._id }}?_method=DELETE" method="post">
    <div class="card" style="width: 18rem;">
        <div class="card-body">
            <h5 class="card-title">{{ result.txtName }}</h5>
            <h6 class="card-subtitle mb-2 text-muted">{{ result.txtEstado}}</h6>
            <p class="card-text">{{ result.txtCharacter}}.</p>
            {{!-- <a href="/editar/{{_id}}" class="card-link">Editar</a> --}}
            {{!-- <a href="eliminar/{{ result._id }}" class="card-link">Eliminar</a> --}}
            <button type="submit" id="btnSubscribe">Eliminar</button>
        </div>
    </div>
</form>

</main>